[package]
name = "flui-widgets"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Headless, unstyled widget primitives for flui — zero visual styling, pure behavior"

[dependencies]
flui-core = { path = "../flui-core" }
smallvec = "1"

[lints]
workspace = true
