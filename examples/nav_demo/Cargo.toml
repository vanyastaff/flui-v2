[package]
name = "nav_demo"
version = "0.1.0"
edition.workspace = true
publish = false

[dependencies]
flui-core = { path = "../../crates/flui-core" }
flui-navigator = { path = "../../crates/flui-navigator" }
