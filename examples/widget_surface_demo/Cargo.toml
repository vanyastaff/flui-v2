[package]
name = "widget_surface_demo"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
# SF01 trait-surface example — depends on flui-framework only. Tier C
# users speak Framework, not Engine. No flui-core / flui-platform / etc.
# direct dependencies.
flui-framework = { path = "../../crates/flui-framework" }

[lints]
workspace = true
